### developing local

```json
{
    "ai-sdk-provider-goose-web": "file:../../evanf/ai-sdk-provider-goose-web",
}
```

remove `--turbopack` from npm run dev

npm install --force

restart dev server

###