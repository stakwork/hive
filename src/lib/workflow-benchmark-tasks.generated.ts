/**
 * Workflow Editor Benchmark corpus index — auto-generated file. DO NOT EDIT.
 * 7 tasks.
 * Source: benchmarks/workflow-editor/tasks/{dir}/{task-slug}/task.json
 * Regenerated by: scripts/generate-workflow-benchmark-tasks.ts
 * (npm run generate:workflow-benchmark-tasks)
 *
 * expected_output is deliberately NOT part of this index — see
 * ./workflow-benchmarks/expected-outputs.server.generated.ts (server-boundary
 * only; never imported by client-reachable code).
 */

import type { WorkflowBenchmarkTask } from "./workflow-benchmarks/task-schema";

export const WORKFLOW_BENCHMARK_TASKS: WorkflowBenchmarkTask[] = [
  {
    slug: "wfbench/create-openai-call",
    title: "Create a workflow that sends a prompt to an LLM",
    instructions: "Create a workflow that sends a user-supplied prompt to an LLM and returns the model's response.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `prompt`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to an LLM provider", match_criteria: "The workflow performs an outbound call to an external LLM provider to obtain generated text — whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out to an LLM service and would receive the model's response back. FAIL: no step contacts any external model/provider (e.g. the workflow merely echoes a canned local value with no provider interaction)." },
      { id: "C-002", title: "The call targets a chat-completion style endpoint", match_criteria: "The outbound call targets a valid LLM provider's chat/completions endpoint (a resource serving chat-completion requests). The specific provider is not asserted: any credible LLM API offering a chat/completions surface passes. FAIL: the call targets an endpoint incapable of producing a chat-style completion (an unrelated or purely auxiliary resource), or the target cannot be determined from the artifact." },
      { id: "C-003", title: "If an HTTP request step is used, its verb suits a chat completion", match_criteria: "CONDITIONAL criterion: applies ONLY when the workflow performs its provider call through an HTTP Request step. In that case the request verb must be the correct one for creating a chat completion (POST); any other verb (GET, PUT, PATCH, DELETE) fails this criterion. Not applicable when the call is made through a native provider/skill step — such steps manage their own transport and expose no user-visible verb to assert." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes the outbound provider call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "The Authorization header's secret reference (on whatever step performs the outbound provider call) must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. PASS: a well-formed reference such as %%SOME_SECRET_NAME%% appears in the Authorization header. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}, appears in the Authorization header." },
      { id: "C-006", title: "A model/deployment identifier is specified", match_criteria: "A model or deployment identifier is specified visibly in the artifact (e.g. in the configuration of the step that performs the provider call) as a non-empty string — any plausible model/deployment name for whatever provider the agent chose passes. No specific provider or model family is asserted: choosing them is the agent's judgment from the intent alone. FAIL: no model/deployment identifier appears anywhere in the produced workflow, or the identifier field exists but is empty or whitespace-only." },
      { id: "C-007", title: "The caller-supplied prompt reaches the provider call, un-hard-coded", match_criteria: "The caller-supplied prompt genuinely reaches the provider call and is not hard-coded: wherever the call's input payload is assembled, the prompt text arrives by reference — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow produces a different exchange for different prompts. The provider request-body schema (field names, message-array shapes) is not asserted. FAIL: the prompt never enters the call, or it is embedded as a fixed literal (the workflow answers identically no matter what the user supplies), or it is empty." },
      { id: "C-008", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-009", title: "Workflow accepts a caller-supplied input named exactly `prompt`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `prompt`, and the artifact's behavior (the request built in C-007) is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `prompt` and uses it to build the request. FAIL if the workflow hard-codes the prompt text directly in the request body with no `prompt` input reference, or if it declares the input under any other name (e.g. `query`, `userPrompt`) — a rerun supplies the value under the name `prompt` and cannot drive a workflow that reads a different name." },
      { id: "C-010", title: "The run output is a plausible LLM response to the supplied prompt", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `prompt` input — not against the workflow JSON. There is no single correct answer; what is asserted is that the output reads as a plausible LLM-generated response to the supplied prompt: coherent text that is responsive to what the prompt asked. PASS: coherent generated text addressing the prompt. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no generated text, a verbatim echo of the prompt with no answer, or content unresponsive to the prompt.", evaluates: "output" },
    ],
    workflow_input: {"prompt":"In one sentence, explain why the sky is blue."},
  },
  {
    slug: "wfbench/gaia-asian-monarchy-coastline",
    title: "Answer a research question that counts entities satisfying two constraints",
    instructions: "Create a workflow that answers a caller-supplied research question by consulting publicly available sources and returns only the final answer.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `question`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to retrieve information", match_criteria: "The workflow performs at least one outbound call to an external source in order to obtain the information the question asks for — a web search API, a page fetch, or an LLM/skill step capable of consulting external sources, whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out and would receive information back. FAIL: no step contacts any external source (e.g. the workflow returns a hard-coded string, or relies solely on a model's unaided recall with no retrieval step of any kind)." },
      { id: "C-002", title: "Workflow accepts a caller-supplied input named exactly `question`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `question`, and the artifact's behavior is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `question` and uses it to build the request. FAIL if the workflow embeds the question text directly with no `question` input reference, or declares the input under any other name (e.g. `query`, `prompt`, `userQuestion`) — a rerun supplies the value under the name `question` and cannot drive a workflow that reads a different name." },
      { id: "C-003", title: "The caller-supplied question reaches the retrieval call, un-hard-coded", match_criteria: "The caller-supplied `question` value genuinely reaches the step that performs retrieval or answering, and is not hard-coded: wherever that step's input payload is assembled, the question arrives by reference to the declared input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow answers ANY question it is supplied, not just one. The request-body schema (field names, message-array shapes) is not asserted. FAIL: the question value never enters the call, or it is replaced with a fixed literal, or the workflow would return the same answer regardless of the question supplied." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes an outbound call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "Any secret reference on the steps performing outbound calls must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}." },
      { id: "C-006", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-007", title: "The run output is a direct answer, not scaffolding", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. PASS: the output is a direct answer to the question asked — the bare value or a short sentence containing it; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer extracted, a verbatim echo of the question, a refusal, or a statement that the answer could not be determined.", evaluates: "output" },
      { id: "C-008", title: "The run output states the correct answer", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. The reference answer is supplied to the judge alongside this task; it is deliberately not restated here, so that the corpus index shipped to the browser carries no answer key. PASS: the answer conveyed by the output matches that reference answer. Exact formatting is not asserted: a short sentence containing the correct value passes, as do differences in casing, surrounding punctuation, or an equivalent spelling of the same value. A numeric answer must match the reference number exactly; a named answer must name the same entity. FAIL: the output states a different value, hedges between several candidate values without committing to the correct one, or names no answer at all.", evaluates: "output" },
    ],
    workflow_input: {"question":"According to wikipedia, how many Asian countries still have a monarchy and access to the sea in 2021?"},
  },
  {
    slug: "wfbench/gaia-lego-wikipedia-images",
    title: "Answer a research question that counts elements on a specific source page",
    instructions: "Create a workflow that answers a caller-supplied research question by consulting publicly available sources and returns only the final answer.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `question`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to retrieve information", match_criteria: "The workflow performs at least one outbound call to an external source in order to obtain the information the question asks for — a web search API, a page fetch, or an LLM/skill step capable of consulting external sources, whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out and would receive information back. FAIL: no step contacts any external source (e.g. the workflow returns a hard-coded string, or relies solely on a model's unaided recall with no retrieval step of any kind)." },
      { id: "C-002", title: "Workflow accepts a caller-supplied input named exactly `question`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `question`, and the artifact's behavior is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `question` and uses it to build the request. FAIL if the workflow embeds the question text directly with no `question` input reference, or declares the input under any other name (e.g. `query`, `prompt`, `userQuestion`) — a rerun supplies the value under the name `question` and cannot drive a workflow that reads a different name." },
      { id: "C-003", title: "The caller-supplied question reaches the retrieval call, un-hard-coded", match_criteria: "The caller-supplied `question` value genuinely reaches the step that performs retrieval or answering, and is not hard-coded: wherever that step's input payload is assembled, the question arrives by reference to the declared input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow answers ANY question it is supplied, not just one. The request-body schema (field names, message-array shapes) is not asserted. FAIL: the question value never enters the call, or it is replaced with a fixed literal, or the workflow would return the same answer regardless of the question supplied." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes an outbound call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "Any secret reference on the steps performing outbound calls must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}." },
      { id: "C-006", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-007", title: "The run output is a direct answer, not scaffolding", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. PASS: the output is a direct answer to the question asked — the bare value or a short sentence containing it; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer extracted, a verbatim echo of the question, a refusal, or a statement that the answer could not be determined.", evaluates: "output" },
      { id: "C-008", title: "The run output states the correct answer", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. The reference answer is supplied to the judge alongside this task; it is deliberately not restated here, so that the corpus index shipped to the browser carries no answer key. PASS: the answer conveyed by the output matches that reference answer. Exact formatting is not asserted: a short sentence containing the correct value passes, as do differences in casing, surrounding punctuation, or an equivalent spelling of the same value. A numeric answer must match the reference number exactly; a named answer must name the same entity. FAIL: the output states a different value, hedges between several candidate values without committing to the correct one, or names no answer at all.", evaluates: "output" },
    ],
    workflow_input: {"question":"How many images are there in the latest 2022 Lego english wikipedia article?"},
  },
  {
    slug: "wfbench/gaia-mercedes-sosa-albums",
    title: "Answer a research question about an artist's studio album count",
    instructions: "Create a workflow that answers a caller-supplied research question by consulting publicly available sources and returns only the final answer.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `question`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to retrieve information", match_criteria: "The workflow performs at least one outbound call to an external source in order to obtain the information the question asks for — a web search API, a page fetch, or an LLM/skill step capable of consulting external sources, whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out and would receive information back. FAIL: no step contacts any external source (e.g. the workflow returns a hard-coded string, or relies solely on a model's unaided recall with no retrieval step of any kind)." },
      { id: "C-002", title: "Workflow accepts a caller-supplied input named exactly `question`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `question`, and the artifact's behavior is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `question` and uses it to build the request. FAIL if the workflow embeds the question text directly with no `question` input reference, or declares the input under any other name (e.g. `query`, `prompt`, `userQuestion`) — a rerun supplies the value under the name `question` and cannot drive a workflow that reads a different name." },
      { id: "C-003", title: "The caller-supplied question reaches the retrieval call, un-hard-coded", match_criteria: "The caller-supplied `question` value genuinely reaches the step that performs retrieval or answering, and is not hard-coded: wherever that step's input payload is assembled, the question arrives by reference to the declared input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow answers ANY question it is supplied, not just one. The request-body schema (field names, message-array shapes) is not asserted. FAIL: the question value never enters the call, or it is replaced with a fixed literal, or the workflow would return the same answer regardless of the question supplied." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes an outbound call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "Any secret reference on the steps performing outbound calls must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}." },
      { id: "C-006", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-007", title: "The run output is a direct answer, not scaffolding", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. PASS: the output is a direct answer to the question asked — the bare value or a short sentence containing it; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer extracted, a verbatim echo of the question, a refusal, or a statement that the answer could not be determined.", evaluates: "output" },
      { id: "C-008", title: "The run output states the correct answer", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. The reference answer is supplied to the judge alongside this task; it is deliberately not restated here, so that the corpus index shipped to the browser carries no answer key. PASS: the answer conveyed by the output matches that reference answer. Exact formatting is not asserted: a short sentence containing the correct value passes, as do differences in casing, surrounding punctuation, or an equivalent spelling of the same value. A numeric answer must match the reference number exactly; a named answer must name the same entity. FAIL: the output states a different value, hedges between several candidate values without committing to the correct one, or names no answer at all.", evaluates: "output" },
    ],
    workflow_input: {"question":"How many studio albums were published by Mercedes Sosa between 2000 and 2009 (included)? You can use the latest 2022 version of english wikipedia."},
  },
  {
    slug: "wfbench/gaia-survivor-may-winner",
    title: "Answer a research question that filters a roster of people by birth month",
    instructions: "Create a workflow that answers a caller-supplied research question by consulting publicly available sources and returns only the final answer.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `question`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to retrieve information", match_criteria: "The workflow performs at least one outbound call to an external source in order to obtain the information the question asks for — a web search API, a page fetch, or an LLM/skill step capable of consulting external sources, whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out and would receive information back. FAIL: no step contacts any external source (e.g. the workflow returns a hard-coded string, or relies solely on a model's unaided recall with no retrieval step of any kind)." },
      { id: "C-002", title: "Workflow accepts a caller-supplied input named exactly `question`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `question`, and the artifact's behavior is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `question` and uses it to build the request. FAIL if the workflow embeds the question text directly with no `question` input reference, or declares the input under any other name (e.g. `query`, `prompt`, `userQuestion`) — a rerun supplies the value under the name `question` and cannot drive a workflow that reads a different name." },
      { id: "C-003", title: "The caller-supplied question reaches the retrieval call, un-hard-coded", match_criteria: "The caller-supplied `question` value genuinely reaches the step that performs retrieval or answering, and is not hard-coded: wherever that step's input payload is assembled, the question arrives by reference to the declared input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow answers ANY question it is supplied, not just one. The request-body schema (field names, message-array shapes) is not asserted. FAIL: the question value never enters the call, or it is replaced with a fixed literal, or the workflow would return the same answer regardless of the question supplied." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes an outbound call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "Any secret reference on the steps performing outbound calls must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}." },
      { id: "C-006", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-007", title: "The run output is a direct answer, not scaffolding", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. PASS: the output is a direct answer to the question asked — the bare value or a short sentence containing it; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer extracted, a verbatim echo of the question, a refusal, or a statement that the answer could not be determined.", evaluates: "output" },
      { id: "C-008", title: "The run output states the correct answer", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. The reference answer is supplied to the judge alongside this task; it is deliberately not restated here, so that the corpus index shipped to the browser carries no answer key. PASS: the answer conveyed by the output matches that reference answer. Exact formatting is not asserted: a short sentence containing the correct value passes, as do differences in casing, surrounding punctuation, or an equivalent spelling of the same value. A numeric answer must match the reference number exactly; a named answer must name the same entity. FAIL: the output states a different value, hedges between several candidate values without committing to the correct one, or names no answer at all.", evaluates: "output" },
    ],
    workflow_input: {"question":"As of August 2023, who is the only winner of the US version of Survivor to be born in the month of May?"},
  },
  {
    slug: "wfbench/gaia-yankees-1977-at-bats",
    title: "Answer a research question requiring a two-step baseball statistics lookup",
    instructions: "Create a workflow that answers a caller-supplied research question by consulting publicly available sources and returns only the final answer.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `question`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to retrieve information", match_criteria: "The workflow performs at least one outbound call to an external source in order to obtain the information the question asks for — a web search API, a page fetch, or an LLM/skill step capable of consulting external sources, whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out and would receive information back. FAIL: no step contacts any external source (e.g. the workflow returns a hard-coded string, or relies solely on a model's unaided recall with no retrieval step of any kind)." },
      { id: "C-002", title: "Workflow accepts a caller-supplied input named exactly `question`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `question`, and the artifact's behavior is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `question` and uses it to build the request. FAIL if the workflow embeds the question text directly with no `question` input reference, or declares the input under any other name (e.g. `query`, `prompt`, `userQuestion`) — a rerun supplies the value under the name `question` and cannot drive a workflow that reads a different name." },
      { id: "C-003", title: "The caller-supplied question reaches the retrieval call, un-hard-coded", match_criteria: "The caller-supplied `question` value genuinely reaches the step that performs retrieval or answering, and is not hard-coded: wherever that step's input payload is assembled, the question arrives by reference to the declared input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow answers ANY question it is supplied, not just one. The request-body schema (field names, message-array shapes) is not asserted. FAIL: the question value never enters the call, or it is replaced with a fixed literal, or the workflow would return the same answer regardless of the question supplied." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes an outbound call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "Any secret reference on the steps performing outbound calls must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}." },
      { id: "C-006", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-007", title: "The run output is a direct answer, not scaffolding", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. PASS: the output is a direct answer to the question asked — the bare value or a short sentence containing it; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer extracted, a verbatim echo of the question, a refusal, or a statement that the answer could not be determined.", evaluates: "output" },
      { id: "C-008", title: "The run output states the correct answer", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `question` input — not against the workflow JSON. The reference answer is supplied to the judge alongside this task; it is deliberately not restated here, so that the corpus index shipped to the browser carries no answer key. PASS: the answer conveyed by the output matches that reference answer. Exact formatting is not asserted: a short sentence containing the correct value passes, as do differences in casing, surrounding punctuation, or an equivalent spelling of the same value. A numeric answer must match the reference number exactly; a named answer must name the same entity. FAIL: the output states a different value, hedges between several candidate values without committing to the correct one, or names no answer at all.", evaluates: "output" },
    ],
    workflow_input: {"question":"How many at bats did the Yankee with the most walks in the 1977 regular season have that same season?"},
  },
  {
    slug: "wfbench/generate-capital-city",
    title: "Create a workflow that answers a country's capital city",
    instructions: "Create a workflow that accepts a country name as a caller-supplied input and returns that country's capital city.\n\n## Workflow Inputs\n\nDeclare each of the following as a caller-supplied workflow input, using these exact names:\n\n- `country`",
    criteria: [
      { id: "C-001", title: "Performs an outbound call to an LLM provider", match_criteria: "The workflow performs an outbound call to an external LLM provider to obtain generated text — whether via an HTTP Request step or an equivalent provider/skill step of the builder environment. PASS: at least one step, of whatever type, demonstrably sends data out to an LLM service and would receive the model's response back. FAIL: no step contacts any external model/provider (e.g. the workflow merely returns a hard-coded or locally computed string with no provider interaction)." },
      { id: "C-002", title: "The call targets a chat-completion style endpoint", match_criteria: "The outbound call targets a valid LLM provider's chat/completions endpoint (a resource serving chat-completion requests). The specific provider is not asserted: any credible LLM API offering a chat/completions surface passes. FAIL: the call targets an endpoint incapable of producing a chat-style completion (an unrelated or purely auxiliary resource), or the target cannot be determined from the artifact." },
      { id: "C-003", title: "If an HTTP request step is used, its verb suits a chat completion", match_criteria: "CONDITIONAL criterion: applies ONLY when the workflow performs its provider call through an HTTP Request step. In that case the request verb must be the correct one for creating a chat completion (POST); any other verb (GET, PUT, PATCH, DELETE) fails this criterion. Not applicable when the call is made through a native provider/skill step — such steps manage their own transport and expose no user-visible verb to assert." },
      { id: "C-004", title: "Authorization header contains no plaintext credential", match_criteria: "On whatever step makes the outbound provider call, the Authorization header value must not embed live credential material of any kind. PASS: the Authorization value is a secret reference in the authoring form %%SOME_SECRET_NAME%% — optionally Bearer-wrapped, provided the wrapped token is itself a %%SOME_SECRET_NAME%%-form reference rather than a literal string. Any well-formed %%[A-Z0-9_]+%% name passes; this criterion never asserts WHICH secret name is used. FAIL: the Authorization header contains a plaintext API key (for example a literal beginning sk-) or a raw key embedded directly in the URL query string or path." },
      { id: "C-005", title: "Secret references use the required authoring form", match_criteria: "The Authorization header's secret reference (on whatever step performs the outbound provider call) must use the required authoring form %%[A-Z0-9_]+%% — uppercase letters, digits and underscores between paired %% markers — and never the rejected runtime form {{ SOME_SECRET_NAME }}. The authoring form %%…%% and the runtime form {{ … }} are two spellings of the same secret-substitution mechanism; %%…%% is required in authored workflow JSON. This criterion asserts only the SHAPE of the reference: it never asserts WHICH secret name is used, and an unresolved %%SOME_SECRET_NAME%% is a warning upstream, never a failure here. PASS: a well-formed reference such as %%SOME_SECRET_NAME%% appears in the Authorization header. FAIL: any malformed reference — a lowercase or hyphenated name such as %%my-secret-name%%, surrounding spaces such as %% NAME %%, or a single-percent variant such as %NAME% — or the rejected runtime form {{ SOME_SECRET_NAME }}, including defaulted variants such as {{ SOME_SECRET_NAME | default: '' }}, appears in the Authorization header." },
      { id: "C-006", title: "A model/deployment identifier is specified", match_criteria: "A model or deployment identifier is specified visibly in the artifact (e.g. in the configuration of the step that performs the provider call) as a non-empty string — any plausible model/deployment name for whatever provider the agent chose passes. No specific provider or model family is asserted: choosing them is the agent's judgment from the intent alone. FAIL: no model/deployment identifier appears anywhere in the produced workflow, or the identifier field exists but is empty or whitespace-only." },
      { id: "C-007", title: "The caller-supplied country reaches the provider call, un-hard-coded", match_criteria: "The caller-supplied country value genuinely reaches the provider call and drives the answer: wherever the call's input payload is assembled, the country arrives by reference to the declared `country` input — [#(step_id).output.variable_name] step-output form or equivalent variable wiring — so the workflow can return the capital of ANY country it is supplied, not just one. The provider request-body schema (field names, message-array shapes) is not asserted. FAIL: the country value never enters the call, or it is replaced with a fixed literal such as \"Wales\", or the user-side prompt does not ask for the capital of the supplied country." },
      { id: "C-008", title: "Workflow accepts a caller-supplied input named exactly `country`", match_criteria: "The workflow declares and consumes a caller-supplied workflow input named exactly `country`, and the artifact's behavior (the request built in C-007) is driven by that input's value rather than a literal. This is assessed at the artifact level: PASS if the produced workflow JSON references an input/variable named `country` and uses it to build the request. FAIL if the workflow hard-codes a specific country (e.g. \"Wales\") directly in the request body with no `country` input reference, or if it declares the input under any other name (e.g. `nation`, `countryName`) — a rerun supplies the value under the name `country` and cannot drive a workflow that reads a different name." },
      { id: "C-009", title: "Workflow is structurally valid", match_criteria: "The workflow JSON satisfies all three structural requirements: (1) there is at least one connection whose source is \"start\" (or the workflow has a designated start node/step); (2) every step is reachable via an unbroken chain of connections from start (no orphaned steps); (3) there is at least one edge whose target is \"system.succeed\" (or the equivalent terminal node), ensuring the workflow can reach a successful completion state. A workflow missing any one of these three is structurally invalid and fails this criterion." },
      { id: "C-010", title: "The run output is a valid capital city", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `country` input — not against the workflow JSON. PASS: the output names a real capital city — the bare city name or a short sentence containing one; casing and surrounding phrasing are not asserted. FAIL: the output is empty, an error message or stack-trace payload, raw request/response scaffolding with no answer, or text that names no capital city.", evaluates: "output" },
      { id: "C-011", title: "The run output is the capital of the supplied country", match_criteria: "Judged against the RUN OUTPUT produced by executing the workflow with the supplied `country` input — not against the workflow JSON. PASS: the capital named in the output is the capital of the country supplied via the `country` input (for `country` = \"Wales\", that is Cardiff); exact formatting is not asserted, and a short sentence containing the correct capital passes. FAIL: the output names a different city (including a non-capital city of the right country, or the capital of a different country), or no city at all.", evaluates: "output" },
    ],
    workflow_input: {"country":"Wales"},
  },
];
